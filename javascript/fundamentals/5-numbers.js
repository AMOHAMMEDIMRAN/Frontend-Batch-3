const num1 = 100;
const num2 = 50;


let val;

val = num1 + num2;
val = num1 - num2;
val = num1 / num2;
val = num1 % num2;

// Math objects

val = Math.PI;
val = Math.round(2.4)
val = Math.ceil(3.1)
val = Math.floor(3.9)
val = Math.sqrt(25)
val = Math.abs(-5)
val = Math.min(-2,4,2,5,7)
val = Math.max(-2,4,2,5,7)
val = Math.random()


val = Math.floor(Math.random() * 100)


console.log(val)